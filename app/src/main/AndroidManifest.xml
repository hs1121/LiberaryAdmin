<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.liberaryadmin">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LiberaryAdmin">
        <activity android:name=".ViewCustomerActivity"></activity>
        <activity android:name=".NewIssueActivity" />
        <activity android:name=".CustomerListActivity" />
        <activity android:name=".AddCustomerActivity" />
        <activity android:name=".AddBookActivity" />
        <activity android:name=".BookListActivity" />
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>